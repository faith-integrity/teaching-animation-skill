# 验证记录

日期：2026-09-25。此记录针对 skill 包及工具，不代表新制作了一支教学动画。

## 已执行

- skill-creator 的 `quick_validate.py`：通过，frontmatter 和名称有效。
- Python 3.10.9 编译检查：三个辅助脚本和测试文件通过。
- `python -m unittest discover -s tests -v`：11 项通过，覆盖无声示例、封面偏移、重复 ID、非有限时间、镜头/cue 重叠与越界、动作越界、缺失音频、WAV 实测时长和尾句超出 cue。
- `doctor.py` 实际运行：能报告所选解释器的依赖和外部命令；不把其他虚拟环境已安装的库误当作当前解释器依赖。
- `check_timeline.py` 对附带 JSON 示例运行：通过。
- `transcribe.py --help`：参数入口可运行。
- Markdown 本地引用检查：通过；检查包内文本未出现来源项目的个人用户名、绝对路径、私有仓库地址或专用音色名称。

## 工作流桌面审查

| 请求情境 | 对应处理规则 |
|---|---|
| “先给完整预览” | 允许全片低清草案，不强制逐镜头批准；保留未确认假设 |
| “字幕很多，拆成 C1/C2” | 按语义拆 cue；确认后不按标点二次拆分 |
| “只改 S03 的运动，不动声音” | 限定修改范围，用现有声音锚点调整，审核后合回 |
| “按我的草图画，位置还没确定” | 静态关键帧和对照先行，未确定部分明确标出 |
| “长句最后几个字没读出来” | 核对 speech_text，语义拆段和定点重录，重新测时 |
| “已有音色继续用” | 检查既有 profile 和服务，先试听，不默认重训 |
| “把所有字母下标统一一下” | 先区分说明性下标和变量索引，避免全局误改 |
| “这几个镜头通过了，合回去” | 复用同一实现，依批准版本清单装配，从最终文件验收 |

以上是规则审查，不是独立代理端到端实验，也不是用户对未来作品的审核。

## 尚未在本包中端到端验证

- 新机器上的依赖安装、Manim 实际渲染，以及非 Manim 引擎集成。
- 本转写脚本连接真实模型后的完整识别质量；此次只验证 CLI 和语法。
- 新 TTS API 适配、参考音色合成和训练效果。本包提供工作流指导，不内置配音后端。
- 主观试听、最终视频观看以及跨平台完整执行。

接入实际动画项目时，按环境指南执行最小渲染、短试听和短转写，再继续生产。公开包不包含来源项目的个人录音、音色权重、教材素材或硬编码个人路径。
